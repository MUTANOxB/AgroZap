import { PagePlaceholder } from "@/components/page-placeholder";

export default function EstoquePage() {
  return (
    <PagePlaceholder
      title="Estoque"
      description="Acompanhe o que entrou, o que foi usado e o que está acabando, como sementes, adubo, produtos para pulverização, ração e combustível."
    />
  );
}
