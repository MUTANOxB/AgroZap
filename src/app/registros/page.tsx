import { PagePlaceholder } from "@/components/page-placeholder";

export default function RegistrosPage() {
  return (
    <PagePlaceholder
      title="Anotações"
      description="Anote de forma simples o que foi feito, onde foi feito, quando aconteceu e quais produtos ou materiais foram usados."
    />
  );
}
